{
  "name": "inventra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/server.ts --compile --minify --bytecode --outfile dist/inventra",
    "typecheck": "bun tsc --noEmit"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
